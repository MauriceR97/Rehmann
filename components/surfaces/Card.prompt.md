Soft, warm-shadowed rounded container — the base surface across the system.

```jsx
<Card elevation="sm" padding="lg">…</Card>
<Card interactive elevation="md">Lifts on hover</Card>
```

Props: `padding` (none/sm/md/lg), `elevation` (none/sm/md/lg), `interactive` (hover lift). Never sharp corners or hard shadows.
